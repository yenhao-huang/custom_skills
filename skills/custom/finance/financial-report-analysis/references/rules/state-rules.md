# State Rules

Reset `STATE.md` from the template for each new execution. Mark a step
`in_progress` before acting and mark it `completed`, `blocked`, or `skipped`
only with concrete evidence. Record companies, reporting periods, source URLs, formulas, output paths, commit result, and push status.
