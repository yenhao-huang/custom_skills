# Filetree Rules

Keep `SKILL.md` and `STATE.md` at the skill root. Keep cron metadata in
`references/cron.md`, operating rules in `references/rules/`, and the reusable
state template in `references/template/STATE.template.md`. Do not add
`agents/`, a top-level `scripts/` directory, reports, or generated cache files.
