# State Rules

Reset `STATE.md` from the template for each new execution. Mark a step
`in_progress` before acting and mark it `completed`, `blocked`, or `skipped`
only with concrete evidence. Record task type, environment, commands, asset identifiers, revisions, validation, and unresolved errors.
