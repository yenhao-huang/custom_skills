# State Rules

Reset `STATE.md` from the template for each new execution. Mark a step
`in_progress` before acting and mark it `completed`, `blocked`, or `skipped`
only with concrete evidence. Record model, flags, readiness, benchmark counts, scored denominators, artifacts, speed metrics, and Git status.
